CREATE TABLE tt_content (
	tx_cfsc_responsive VARCHAR(255) DEFAULT NULL,
	tx_cfsc_isgallery tinyint(4) DEFAULT '0' NOT NULL,
);

CREATE TABLE tt_content (
	tx_cfsc_responsive VARCHAR(255) DEFAULT NULL,
	tx_cfsc_isgallery tinyint(4) DEFAULT '0' NOT NULL,
);

CREATE TABLE sys_file_metadata (
  tx_cfsc_responsive text DEFAULT '' NOT NULL,
);

CREATE TABLE sys_file_reference (
  tx_cfsc_responsive text DEFAULT '' NOT NULL,
);
